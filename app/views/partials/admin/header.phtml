<!-- Header -->
<div id="admin-header">
    <header class="clearfix">
        <div class="float-left title">
            Barn Admin<?php echo ( isset( $subPage ) ) ? '<span>&raquo;</span>'. $subPage : ''; ?>
        </div>
        <div class="float-right">
            <a class="gray button fontsize-18" href="<?php echo $this->url->get( $backPage ); ?>">
                &larr;
                Back
            </a>
<?php   if ( isset( $buttons ) ):
            foreach ( $buttons as $button ):
                if ( $button == 'new' ): ?>
            <a class="blue button fontsize-18 margin-left-10" href="<?php echo $this->url->get( 'admin/new' ); ?>">New Article</a>
<?php           endif;
                if ( $button == 'save' ): ?>
            <a class="green button fontsize-18 margin-left-10" href="javascript:;" id="save-article">Save Article</a>
<?php           endif;
            endforeach;
        endif; ?>
        </div>
    </header>
</div>

<!-- Messages -->
<?php   if ( $flashMessages ): ?>
<div id="admin-messages">
<?php       foreach ( $flashMessages as $message ): ?>
    <div class="message <?php echo key( $message ); ?>"><?php echo current( $message ); ?></div>
<?php       endforeach; ?>
</div>
<?php   endif; ?>