<!-- Hero -->
<div id="hero" class="hero clearfix">
<?php
$i = 0;
foreach ( $posts as $post ):
    $class = ( $i === 0 ) ? 'major' : 'minor';
    if ( $i >= 1 && $i <= 2):
        $class .= ' bottom-border';
    elseif ( $i >= 3 || $i <= 4 ):
        $class .= ' top-border';
    endif;
    $i++; ?>
    <a class="<?php echo $class; ?> box img-wrap"
        style="background-image:url('<?php echo $post->getImage()->getImagePath(); ?>');"
        href="<?php echo $post->getPath(); ?>">
        <h1>
            <span>
                <?php echo ( strlen( $post->title ) > 96 ) ? substr( $post->title, 0, 96 ) . "&hellip;" : $post->title; ?>
            </span>
        </h1>
    </a>
<?php
endforeach; ?>
</div>

<!-- Sign up for mailing list -->
<div id="cta" class="clearfix">
    <div class="box">
        <h1 class="centered-text uppercase font-sans">
            Support the Barn
        </h1>
        <div class="centered-text">
            <a class="green button" href="javascript:;">Donate Today :)</a>
        </div>
    </div>
    <div class="box">
        <h1 class="centered-text uppercase font-sans">
            Signup for our Newsletter
        </h1>
        <div class="centered-text">
            <input type="text" name="email" value="" placeholder="Your email" />
            <a class="blue button" href="javascript:;">Sign Up</a>
        </div>
    </div>
</div>